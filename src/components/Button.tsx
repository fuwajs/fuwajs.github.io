import styled from 'styled-components';

export const BaseButton = styled.button`
    border-radius: 1rem;
    width: 10%;
`;

export const GradiantButton = styled(BaseButton)`
    background-color: var(--bg);
`;
